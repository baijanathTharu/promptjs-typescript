/**
 * ! Please don't use `any` in Typescript
 */

// > Typescript code that uses `any` is as good as Javascript code

// Let's check the `cv` code again and
// assume it is written with bunch of any's
// Check `test.js` file

// ? Can you guess what will happen ?

export {};
